Control Flow
    เป็นการเขียนชุดคำสั่งให้โปรแกรมจัดสินใจเลือกวิธีการทำงานตามเงื่อนไขที่กำหนด
    โดยที่นิยมใช้ คือ if, else if, else, match 

    ** match จะคล้ายกับ swtch case แต่จะส่งคืนค่าออกมาจากแต่ละเงื่อนไขได้